Лабораторна робота: CI/CD з безпековими перевірками
🛡️Цей етап є міні-практикою, де ви побачите, як працює механізм «Fail Fast», і навчитеся читати результати безпекових сканерів. Це перша, суто практична фаза, де ви збираєте факти та спостерігаєте за реакцією пайплайну на вразливості.


Мета: Навчитися будувати CI/CD-пайплайн з інтегрованими етапами безпеки (secret scanning, SAST, SCA) та зрозуміти, як пайплайн реагує на вразливості. Ознайомитися з реальним підходом «fail fast» у DevSecOps.

У рамках лабораторної роботи використовується демонстраційний застосунок, який імітує типову вебсистему для керування замовленнями. Застосунок написаний мовою програмування Python з використанням фреймворку Flask. Він складається з REST API та простої вебформи для введення даних.



Підготовка середовища

Зареєструйтесь у GitLab (якщо ще немає акаунта). Якщо акаунт є, авторизуйтесь.

1. Створіть проєкт.

2. Додайте файли з архіву «CICD_DevSecOps»:

.gitlab-ci.yml
Dockerfile
py
txt
test_app.py



4. ! Файл test_app.py візьміть з теки «Good».

5. У вас має бути такий вигляд репозиторію:






Хід роботи

Крок 1. Ознайомлення зі структурою репозиторію

1. Відкрийте репозиторій у GitLab.

2. Перегляньте вміст файлів app.py, test_app.py, .gitlab-ci.yml.

3. Зрозумійте, які етапи CI/CD вже налаштовані:

secret-scan — перевірка наявності секретів;
sast — статичний аналіз коду;
test — запуск тестів;
build — збірка застосунку;
sca — перевірка залежностей;
deploy — розгортання;
dast — динамічний аналіз безпеки.


Крок 2. Запуск початкового пайплайну

1. Виконайте коміт (наприклад, дрібна зміна в README.md), щоб запустився пайплайн.

2. Дочекайтеся завершення пайплайну.

3. Відкрийте вкладку Build → Pipelines у GitLab.

4. Перегляньте результати:

які етапи виконались успішно;
чи були виявлені вразливості (SAST, SCA, Secret Scanning);
зверніть увагу, що пайплайн завершився успішно, попри наявність низькорівневих уразливостей.





5. Проаналізуйте логи кожного етапу:

secret-scan;
sast;
test;
build;
sca;
deploy;
dast.
6. Знайдіть низькорівневі вразливості. Ознайомтеся з ними. Розберіться, як вони впливають на застосунок.



Крок 3. Імітація критичної вразливості

1. Замініть файл test_app.py на екземпляр з теки «Bed». Можна скопіювати код і замінити в репозиторії GitLab.

2. Закомітьте зміну й запустіть пайплайн.

3. Дочекайтеся завершення перевірок.



Крок 4. Аналіз результатів другого запуску

1. Відкрийте вкладку Pipeline та порівняйте з попереднім успішним запуском.

2. Зверніть увагу:

на якому етапі пайплайн зупинився;
яке повідомлення про помилку видало SAST / SCA;
що пайплайн фейлиться у випадку виявлення серйозної вразливості (Fail Fast).


﻿Крок 5. Звіт (використовуватиметься при виконанні ДЗ)

1. Запустіть пайплайн з початковим, умовно безпечним кодом та зробіть скриншот результатів.

2. Модифікуйте test_app.py, щоб з’явилася критична вразливість, і повторно запустіть пайплайн. Зробіть скриншот результатів.

3. Порівняйте два результати запуску.

4. Дайте письмову відповідь на запитання:

Чому в першому випадку пайплайн завершився успішно?
Чому у другому випадку він зупинився?
У чому різниця між першим і другим випадком?
Чи дійсно перший випадок безпечний і немає вразливостей?
Як це ілюструє принцип fail fast у DevSecOps?


 ❗Тепер переходимо до наступної, аналітичної частини в домашньому завданні 🙂
